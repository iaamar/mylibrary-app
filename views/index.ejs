<% if(books.length>0){ %>
    <h2 class="page-header">
        Recently Added
    </h2>
    <%- include('partials/bookGrid',{books:books }) %>
        <% } else { %>
            <h2>No Book in inventory</h2>
            <% } %>